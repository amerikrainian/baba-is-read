# Native build output
build/

# Runtime files the dev server writes into the deployed mod (never in the repo)
lua/baba_access/cmd/

# Python
__pycache__/
.venv/

# Release artifacts (build_release.ps1, build-installer.ps1), the staging dir, the installer's cargo output
releases/
obj/
installer/target/
